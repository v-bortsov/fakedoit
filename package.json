{
  "name": "rn-nodejs-monorep",
  "version": "1.0.0",
  "description": "Starter for Monorepos: Lerna, TypeScript, CRA and Storybook",
  "repository": "https://github.com/v-bortsov/react-typescript-data-gen.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start --parallel --stream --scope fakedoit --scope pm2graphql",
    "web:start": "lerna run start --parallel --stream --scope fakedoit --scope fakedoit",
    "sonar": "node ./packages/client/sonar-project.js",
    "test": "lerna run test --stream"
  },
  "dependencies": {
    "typescript": "^4.4.3"
  }
}
